# Hello Name

Simple Node app that prompts for your name and prints it back with "Hello".

Run:

PowerShell or CMD:

```bash
node index.js
# or
npm start
```

You can also test non-interactively:

```bash
echo Alice | node index.js
```